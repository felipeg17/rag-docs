apiVersion: v1
kind: Service
metadata:
  name: rag-docs-frontend
  labels:
    app: rag-docs-frontend
spec:
  type: ClusterIP
  ports:
    - port: 8502
      targetPort: 8502
      protocol: TCP
      name: http
  selector:
    app: rag-docs-frontend
