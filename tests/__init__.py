"""
Unit tests for RAG-docs application.

Testing Strategy:
- Unit tests with mocked external dependencies
- Golden fixtures for deterministic responses (embeddings, VectorDB, reranking)
- pytest + pytest-mock framework
"""
